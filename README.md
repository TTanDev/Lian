# Lian

Pure SwiftUI rewrite of the Lian iOS app.

## Generate the Xcode project

```sh
brew install xcodegen
xcodegen generate
open Lian.xcodeproj
```

The app targets iOS 26 and uses SQLite directly through `SQLite3`.
